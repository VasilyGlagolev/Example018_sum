// Задача 34: Задайте массив заполненный случайными положительными трёхзначными числами. 
// Напишите программу, которая покажет количество чётных чисел в массиве.
// [345, 897, 568, 234] -> 2

using System;

class Program
{
    static void Main(string[] args)
    {
        Console.Write("Введите количество чисел в массиве: ");
        int n = int.Parse(Console.ReadLine());

        int[] array = new int[n];

        // Заполнение массива положительными трёхзначными числами, введёнными пользователем
        for (int i = 0; i < array.Length; i++)
        {
            int number;
            do
            {
                Console.Write($"Введите {i + 1}-е трёхзначное число: ");
                number = int.Parse(Console.ReadLine());
            } while (number < 100 || number > 999);
            array[i] = number;
        }

        int count = 0;

        // Подсчёт количества чётных чисел в массиве
        foreach (int number in array)
        {
            if (number % 2 == 0)
            {
                count++;
            }
        }

        Console.WriteLine($"Количество чётных чисел в массиве: {count}");
    }
}


// Программа запрашивает у пользователя количество чисел в массиве и создаёт массив нужного размера. 
// Затем она запрашивает у пользователя положительные трёхзначные числа и сохраняет их в массив. 
// При этом программа проверяет, что число действительно является трёхзначным и положительным, и продолжает запрашивать число,
// пока пользователь не введёт корректное значение.

// Далее программа перебирает все числа в массиве и считает количество чётных чисел, увеличивая счётчик count каждый раз, 
// когда находит чётное число. В конце программа выводит количество чётных чисел на экран.